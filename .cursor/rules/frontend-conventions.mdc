---
description: Convenções do frontend Next.js/React/TypeScript
globs: "frontend/**/*.{ts,tsx}"
alwaysApply: false
---

# Frontend — Convenções

## Stack

- Next.js 16 (App Router) com TypeScript
- React 19 com componentes funcionais
- Tailwind CSS 4 para estilização
- TanStack React Query 5 para data fetching e cache
- Recharts 3 para visualizações/gráficos
- Leaflet + react-leaflet para mapas interativos (ex.: concílios)
- Lucide React para ícones
- next-themes para tema (light/dark/system)
- next-intl para internacionalização (pt, en, es)
- Prometheus + Grafana + Loki para observabilidade

## Estrutura

```
frontend/
├── app/[locale]/          Páginas (37 rotas) — App Router com i18n
├── components/
│   ├── bible/              BibleNavigation, BibleReader, BibleSearchBar, BibleSearchResults, InterlinearView, LinkedVerseReader, StrongsConcordance, StrongsEntry, StudySidebar, VerseCompare
│   ├── charts/             ComparisonChart, CouncilsTimelineChart, FathersTimelineChart, Heatmap, TimelineChart
│   ├── councils/           CouncilMapView, CouncilTypeBadge, SourceProvenancePanel, SummaryToggle
│   ├── coverage/           BookCard, CenturySlider, GospelPanel, VerseGrid
│   ├── filters/            CenturyRangeFilter, YearRangeFilter, YearSliderFilter
│   ├── ingestion/          BibleIngestionPanel, CouncilIngestionPanel, IngestionPhasePanel, LlmQueuePanel, ManuscriptIngestionPanel, PatristicIngestionPanel
│   ├── layout/             Header, LanguageSelector, Sidebar, SidebarContext, ThemeToggle
│   ├── llm/                ErrorDetailModal, LlmProviderCard, LlmRateLimitPanel, LlmUsageTable
│   ├── observatory/        AuthGate, ExplorerTab, OverviewTab, SessionDrawer, TimelineTab, VisitorTracker, VisitorsTab
│   ├── stats/              StatsOverview
│   ├── ui/                 ConfidenceDot, DatingBadge
│   └── providers.tsx       React Query + Theme providers
├── hooks/                 18 hooks customizados
├── i18n/                  routing.ts, navigation.ts, request.ts (config next-intl)
├── lib/                   api/ (módulos por domínio), utils.ts, verseReference.ts
├── types/                 index.ts (interfaces/tipos)
└── messages/              en.json, pt.json, es.json
```

## Padrões

- Componentes funcionais com TypeScript
- Hooks customizados em `frontend/hooks/` (um por domínio)
- API layer modular em `frontend/lib/api/` (um arquivo por domínio)
- Tipos distribuídos em `frontend/types/` (um arquivo por domínio + index.ts para exports)
- Preferir Server Components; usar `"use client"` apenas quando necessário
- Tipagem explícita — evitar `any`

## Data Fetching

- Usar TanStack Query (`useQuery`, `useMutation`) para chamadas à API
- API backend disponível via rewrite `/api/*` -> `http://app:8080` (Docker) ou `http://localhost:8080` (dev)
- Funções de fetch centralizadas em `lib/api/` (modular por domínio)
- Axios instance com interceptors para auth token em `lib/api/client.ts`

## Estilização

- Tailwind CSS utility-first
- Sem CSS modules ou styled-components
- Dark mode via `dark:` prefix

## Responsividade

- Todo componente e página deve funcionar em mobile (>=320px), tablet e desktop
- Dashboards/admin: usar `max-w-7xl` com `mx-auto w-full`
- Breakpoints: `sm:` (640px), `md:` (768px), `lg:` (1024px), `xl:` (1280px)
- Grid adaptativo: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
- Tabelas largas: `overflow-x-auto`
- Sidebar colapsa em telas pequenas
- Textos escaláveis: `text-sm md:text-base`, `p-4 md:p-6`

## Internacionalização

- **Frontend** (labels, botões, UI): `messages/{locale}.json` + `useTranslations()`
- **Backend** (dados de domínio): API retorna textos traduzidos via `?locale=` e tabelas `*_translations`
- Todas as rotas sob `[locale]/`
- Idiomas: pt (português), en (inglês), es (espanhol)
- **Nenhum texto visível ao usuário deve ser hardcoded**
- Ao adicionar features, atualizar os 3 arquivos simultaneamente

## Checklist por feature

1. **i18n**: todos os textos nos 3 locales?
2. **Responsividade**: mobile (320px), tablet (768px), desktop (1280px)?
3. **Tema**: light e dark mode?
4. **Tipos**: TypeScript sem `any`, interfaces nos arquivos de tipos?
5. **Loading/Error**: estados tratados?

## Páginas (37 rotas)

**Atlas (cobertura/manuscritos):**
Dashboard, Book/Name, Compare, Timeline, Metrics, Metrics/Book, Manuscript-Count, Manuscripts, Manuscripts/GaId, Verse-Lookup, History, Sources, Methodology, FAQ

**Patrística:**
Fathers, Fathers/Id, Fathers/Testimony, Patristic-Dashboard

**Concílios:**
Councils, Councils/Slug, Heresies, Heresies/Slug

**Bíblia:**
Bible (home), Bible/Search, Bible/Compare, Bible/Interlinear, Bible/Strongs/[number]

**Apologética:**
Apologetics, Apologetics/[slug], Apologetics/New

**Admin:**
Admin/Users, Ingestion-Status, Observatory, LLM-Usage, Wiki-LLM

**Outros:**
Home (redirect), Manifesto

## Componentes de ingestão (padrão reutilizável)

- `IngestionPhasePanel` — componente genérico (status, progresso, botões run/reset)
- `ManuscriptIngestionPanel`, `PatristicIngestionPanel`, `CouncilIngestionPanel`, `BibleIngestionPanel` — painéis por domínio que delegam ao genérico
- `LlmQueuePanel` — painel de monitoramento da fila LLM (pending, processing, apply)
- Hooks: `useIngestionPhases.ts` (fases por domínio), `useIngestion.ts` (status global), `useCouncilIngestion.ts` (fases de concílios), `useLlmQueue.ts` (fila LLM)
